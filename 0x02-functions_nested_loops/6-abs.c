#include "main."

/**
 * _abs - print the absolute value of the integer
 * @c: is the int will be for argument of function
 * Retun 0
 */

int _abs(int c)
{
	if (c > 0 || c == 0)
	{
	return (c);
	}
	else
	return (c * -1);
}
